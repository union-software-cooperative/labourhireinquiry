-- <%= rec.union.call_to_action %> --

<%= rec.union.explanation %>

<%= rec.union.submissions_heading %>
<%="https://#{request.host}/#{rec.union.short_name}?source=mail&token=#{rec.token}"%>

Thanks 
<%= rec.person.first_name %>