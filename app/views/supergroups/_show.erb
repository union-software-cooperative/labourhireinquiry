<%=render 'title' %>
<div class="row">
  <div class="col-sm-8 col-sm-offset-2">
		<%=render partial: 'recs/recs' if @recs %>

		<% if current_person %>
			<%=render partial: 'posts/form'%>
			<%=render @supergroup.posts%>
		<% end %>
	</div>
</div>