<div class="follow-container">
<%= link_to current_person.follows?(@supergroup) ? "unfollow" : "follow", follow_polymorphic_path, class: "btn btn-primary btn-follow" %>
</div>
<% if @supergroup.banner.present? %>
	<div class="banner-placeholder">
		<%= banner_image %>
		<%= render 'show' %>
	</div>

<% else %>
	<%= render 'show' %>
<% end %>

